{
  "name": "resa-backend",
  "version": "1.0.0",
  "description": "Gestion des réservations",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -r ./dist/*",
    "build:dist": "npm run clean && babel ./src --out-dir dist --copy-files",
    "start": "nodemon ./src/index.js --exec babel-node -e js",
    "start:dist": "nodemon ./dist/index.js --exec babel-node -e js"
  },
  "author": "Laurent Varciat",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "core.js": "^0.4.2",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "joi": "^17.8.3",
    "mongodb": "^5.1.0",
    "mongoose": "^6.10.0",
    "nodemon": "^2.0.20",
    "regenerator-runtime": "^0.13.11"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}